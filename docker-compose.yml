services:
  app:
    build:
      context: .
      target: development
    init: true
    env_file:
      - .env
    ports:
      - "127.0.0.1:8002:8000"
    volumes:
      - ./app:/usr/src/app
      - ./tests:/usr/src/app/tests
      - ./pyproject.toml:/usr/src/app/pyproject.toml
    depends_on:
      db:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
    restart: unless-stopped
    networks:
      - frontend
      - backend

  db:
    image: postgres:16-alpine
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB is required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"
        ]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      - backend
  
  app-test:
    build:
      context: .
    init: true
    env_file:
      - .env
    environment:
      environment:
      DATABASE_USER: ${POSTGRES_USER_TEST:?POSTGRES_USER_TEST is required}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD_TEST:?POSTGRES_PASSWORD_TEST is required}
      DATABASE_HOST: db-test
      DATABASE_PORT: 5432
      DATABASE_NAME: ${POSTGRES_DB_TEST:?POSTGRES_DB_TEST is required}
    volumes:
      - ./tests:/usr/src/app/tests
    depends_on:
      db-test:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
    networks:
      - backend

  db-test:
    image: postgres:16-alpine
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER_TEST:?POSTGRES_USER_TEST is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD_TEST:?POSTGRES_PASSWORD_TEST is required}
      POSTGRES_DB: ${POSTGRES_DB_TEST:?POSTGRES_DB_TEST is required}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"
        ]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      - backend
    
  adminer:
    image: adminer:5
    profiles:
      - tools
    ports:
      - "127.0.0.1:8080:8080"
    depends_on:
      db:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    networks:
      - backend

volumes:
  postgres_data:

networks:
  frontend:
  backend:
    internal: true