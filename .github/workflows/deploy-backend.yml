name: Deploy Backend to ECS

on:
  push:
    branches:
      - main
    paths:
      - app/models/**
      - app/routers/**
      - app/cruds/**
      - app/schemas/**
      - app/service/**
      - app/alembic/**
      - app/alembic.ini
      - app/db.py
      - app/config.py
      - app/enums.py
      - app/main.py
      - tests/**
      - Dockerfile
      - docker-compose.yml
      - docker-compose.prod.yml
      - requirements.txt
      - .github/workflows/deploy-backend.yml

permissions:
  contents: read

env:
  AWS_REGION: ap-northeast-1
  ECR_REPOSITORY: rin-task-app
  ECS_CLUSTER: rin-task-app-cluster
  ECS_SERVICE: rin-task-app-task-service-05mi790b
  ECS_TASK_DEFINITION: rin-task-app-task
  CONTAINER_NAME: rin-task-app

jobs:
  ci:
    runs-on: ubuntu-latest

    env:
      PYTHONPATH: app

      DATABASE_USER: test_user
      DATABASE_PASSWORD: test_password
      DATABASE_HOST: localhost
      DATABASE_PORT: 5432
      DATABASE_NAME: test_db

      DATABASE_USER_TEST: test_user
      DATABASE_PASSWORD_TEST: test_password
      DATABASE_HOST_TEST: localhost
      DATABASE_PORT_TEST: 5432
      DATABASE_NAME_TEST: test_db

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U test_user -d test_db"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pytests
        run: pytest -v
    
      - name: Run migration upgrade test
        run: |
          cd app
          alembic upgrade head
      
      - name: Run migration downgrade test
        run: |
          cd app
          alembic downgrade base
      
      - name: Run migration upgrade retest
        run: |
          cd app
          alembic upgrade head

  deploy:
    needs: ci

    permissions:
      contents: read
      id-token: write
    
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::538498973932:role/rin-task-app-github-actions-role
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker buildx build \
            --platform linux/arm64 \
            --target production \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            --push \
            .
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> "$GITHUB_OUTPUT"

      - name: Download current task definition
        run: |
          aws ecs describe-task-definition \
            --task-definition $ECS_TASK_DEFINITION \
            --query taskDefinition \
            > task-definition.json

      - name: Render new task definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: task-definition.json
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.build-image.outputs.image }}

      - name: Deploy to Amazon ECS
        id: ecs-deploy
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true

          run-task: true
          run-task-launch-type: FARGATE
          run-task-container-overrides: |
            [
              {
                "name": "${{ env.CONTAINER_NAME }}",
                "command": ["alembic", "upgrade", "head"]
              }
            ]

          run-task-subnets: subnet-0fa4de622fa74de87,subnet-070b331866ae3f9c7
          run-task-security-groups: sg-0b4689362453a9a7a
          run-task-assign-public-IP: ENABLED
          wait-for-task-stopped: true
          
      - name: Show migration task ARN
        run: echo '${{ steps.ecs-deploy.outputs.run-task-arn }}'